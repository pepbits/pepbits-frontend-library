# Host-defined module navigation — 24 September 2026

Added a generic fallback landing page for modules without dashboards and allowed product-specific stable module keys. The shell now shows a static module label when there is one choice and falls back to the product default after access removes the active module. No new permission or API behavior is implied.

Local package typecheck and focused product/context tests are the implementation checks. Publication, deployment and authenticated host acceptance are separate.
